package entity;

import map.TileMap;

public class RoomExitEdge /*extends RoomExit*/ {

    /*public RoomExitEdge(float x, float y, TileMap tm, String spriteLoc,
	    int spriteWidth, int spriteHeight, int collWidth, int collHeight, int transitionType) {
	super(x, y, tm, spriteLoc, spriteWidth, spriteHeight, collWidth, collHeight);
	// TODO Auto-generated constructor stub
    }

    public RoomExitEdge(float x, float y, TileMap tm, String spriteLoc,
	    String controlLoc, int spriteWidth, int spriteHeight,
	    int collWidth, int collHeight) {
	super(x, y, tm, spriteLoc, controlLoc, spriteWidth, spriteHeight,
		collWidth, collHeight);
	// TODO Auto-generated constructor stub
    }*/
}
