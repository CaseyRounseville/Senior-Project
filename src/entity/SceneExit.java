package entity;

import map.TileMap;

public abstract class SceneExit /*extends Exit*/ {
    
    /*String otherScene;
    int otherRoom;

    public SceneExit(float x, float y, TileMap tm, String otherScene, int otherRoom) {
	super(x, y, tm, spriteLoc, spriteWidth, spriteHeight, collWidth,
		collHeight);
	// TODO Auto-generated constructor stub
    }

    public SceneExit(float x, float y, TileMap tm, String spriteLoc,
	    String controlLoc, int spriteWidth, int spriteHeight,
	    int collWidth, int collHeight) {
	super(x, y, tm, spriteLoc, controlLoc, spriteWidth, spriteHeight,
		collWidth, collHeight);
	// TODO Auto-generated constructor stub
    }*/

}
